require 'rails_helper'

feature "User signup" do
  scenario "when user finishes sign up" do
    # visit '/'

    # allow_any_instance_of(Game).to receive(:computer_throw) { 'rocket' }
    # first(".btn-success").click

    # expect(page).to have_content("User won")
  end


end