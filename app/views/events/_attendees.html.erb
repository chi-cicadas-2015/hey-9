<div class="col s12 l4">
    <% if dog_events(current_user.dogs).include?(event) %>
      <a class="btn-floating btn-large waves-effect waves-light red"></a>
    <% end %>
    <h5>Attending</h5>
    <div class="row">
      <% if @attendees %>
        <% @attendees.each do |dog| %>
        <%= link_to "#{dog.name}", dog_path(dog.id) %>
      <% end %>
          </div>
        <% end %>
      <% end %>
</div>