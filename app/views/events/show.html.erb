<div class="container">
  <h1>This is <%= @user.username %>'s Event<h1>

  <%= render partial: "event", locals: {event: @event} %>

  <div class="section">
   <% if @event.comments %>
     <ul>

     </ul>
   <% end %>
  </div>
</div>
